services:
  - type: web
    name: midpoint-server
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PROVIDER
        value: osm
      - key: NODE_VERSION
        value: 20.11.0
