PORT=4000
# osm (default, free): real venues via OpenStreetMap + real driving times via OSRM
# mock: fully fake fixture data, no network calls
# google: requires GOOGLE_MAPS_API_KEY and a billing account
PROVIDER=osm
GOOGLE_MAPS_API_KEY=
